<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body>
    <style>
        .menuContainer { width: 15vw; top: 0; left: 0; display: flex; flex-direction: column; align-items: center; justify-content: space-evenly; }
        .menu { width: 100%; display: flex; align-items: center; justify-content: space-evenly; }
        .extra { display: flex; flex-direction: column; justify-content: space-evenly; align-items: center; font-size: 1.25rem; }
        .menuItem { text-decoration: none; font-size: 1.25rem; }
        .quote {width: 60%; margin: 0 auto; text-align: center; font-size: 1.25rem; }
        footer { position: absolute; bottom: 0; width: 100%; height: 10vh; display: flex; justify-content: center; align-items: center; text-align: center; }
    </style>
    <aside class="menuContainer">
        {% block menu %}
            <div class="menu">
                <a class="menuItem" href="{{ path('home') }}">Home</a>
                <a class="menuItem" href="{{ path('aboutMe') }}">About</a>
            </div>
        {% endblock %}
    </aside>
        {% block body %}{% endblock %}
    <section class="quote">
        {{quote()|nl2br}}
    </section>
    <footer>
        {% block footer %}© BeCode{% endblock %}
    </footer>
    </body>
</html>
